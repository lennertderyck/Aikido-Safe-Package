# PackageGuard
Security badges for NPM packages.

### Stack
- Next.js application:
  - Generate and serve SVG badges that are embedded into NPM.
  - Serve a detail page showing the advisories for a package.
- Extension: embeds the SVG badge in a NPM page.
